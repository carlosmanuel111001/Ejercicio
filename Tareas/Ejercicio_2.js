function obtenerLongitud(cadena) {
  // Usamos la propiedad length para obtener la longitud de la cadena
  return cadena.length;
}
// Llamamos a la función con una cadena de texto como argumento
const texto = "Hola, mundo!";
const longitud = obtenerLongitud(texto);

// Imprimimos la longitud de la cadena en la consola
console.log("La longitud de la cadena es: " + longitud);

